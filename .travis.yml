language: c
# Compiler selection
compiler:
  - clang
  - gcc
env:
  - ANALYZE=false
  - ANALYZE=true
# Build steps
script:
  - sh .travis.sh
after_success:
  - cd build && bash <(curl -s https://codecov.io/bash)