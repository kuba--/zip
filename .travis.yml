language: c
# Compiler selection
compiler:
  - clang
  - gcc
# Build steps
script:
  - mkdir build
  - cd build
  - cmake -DCMAKE_BUILD_TYPE=Debug -DSANITIZE_ADDRESS=On .. && make && LSAN_OPTIONS=verbosity=1:log_threads=1 ctest -V
