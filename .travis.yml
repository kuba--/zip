language: c
addons:
  apt:
    packages: &1
    - lcov
    - valgrind
# Compiler selection
compiler:
  - clang
  - gcc
env:
  - ANALYZE=false
  - ANALYZE=true
# Build steps
script:
  - ./.travis.sh
after_success:
  - ./.codecov.sh